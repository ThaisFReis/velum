[package]
name = "seize"
type = "bin"
authors = ["velum"]
compiler_version = ">=0.30.0"

# Path-depends on a local checkout of OpenZeppelin/stellar-contracts at the
# `feat/confidential-verifier-ultrahonk` branch, like the sibling circuits.
[dependencies]
stellar_confidential_lib = { path = "../../refs/oz-stellar-contracts-ct-branch/packages/tokens/src/confidential/circuits/lib" }
